# F4 판매점 지도/리스트 UI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** F4 데이터 파이프라인이 만든 `data/lotto-stores.json`을 소비하는 실제 사용자 화면(`/stores`)을 만든다 — 리스트 뷰(기본) + 지도 뷰(클러스터링) 토글, 위치 기반 거리순 정렬 또는 지역 필터, 판매점 상세(길찾기 링크 포함).

**Architecture:** 순수 로직(거리 계산, 정렬, 지역 추출)은 `lib/stores/distance.ts`에 두어 유닛테스트한다. UI는 리스트 뷰를 먼저 완성해 독립적으로 검증 가능하게 만들고(Task 3), 지도 뷰(Kakao Maps JS SDK + MarkerClusterer)는 별도 태스크(Task 4)로 분리한다 — 브라우저 전용 지도 SDK 연동은 순수 로직/리스트 UI와 성격이 달라 별도 리뷰 대상으로 삼는 게 맞다. Task 3에서는 지도 뷰 자리에 플레이스홀더를 두어 페이지 전체가 컴파일·동작하게 하고, Task 4에서 실제 구현으로 교체한다.

**Tech Stack:** 기존과 동일(Next.js App Router, TS, Tailwind) + Kakao Maps JavaScript SDK(신규 npm 의존성 아님 — `<script>` 태그로 런타임 로드).

## Global Constraints

- 기본 화면은 **리스트 뷰**이며, 정렬 기본값은 **배출횟수순**(1등 배출 많은 순)이다.
- 페이지 로드 시 위치 권한을 자동으로 요청하지 않는다 — "내 위치로 정렬" 버튼을 눌렀을 때만 `navigator.geolocation.getCurrentPosition`을 호출한다.
- 위치 허용 시: 거리순 정렬로 전환되고, 버튼이 `[거리순 | 배출횟수순]` 토글로 바뀐다.
- 위치 거부/미지원 시: 지역(시/도) 드롭다운이 나타나 리스트를 필터링한다(정렬 기준은 배출횟수순 유지). 드롭다운 선택지는 하드코딩하지 않고 로드된 데이터에서 동적으로 추출한다.
- 판매점 카드는 클릭 시 그 자리에서 확장되어 전체 주소·배지 상세·길찾기 링크를 보여준다(별도 모달/페이지 없음).
- 길찾기 링크 형식: `https://map.kakao.com/link/to/{상호명},{lat},{lng}` (API 키 불필요, `target="_blank"`).
- 지도 뷰는 **Kakao Maps `MarkerClusterer`로 마커 클러스터링을 반드시 적용**한다(선택 사항 아님).
- 화면 상단에 정확히 이 데이터 기준일 문구를 표시한다: "판매점 목록은 2025-06-07 기준이며, 폐업·신규 매장이 반영되지 않았을 수 있습니다. 1등 배출 이력은 2025년 6월 기준입니다."
- 배지가 있는 카드를 펼쳤을 때 정확히 이 문구를 함께 표시한다: "자동선택 당첨 기준입니다. 특정 판매점 이용이 당첨 확률에 영향을 주지 않습니다."
- 번호/판매점 관련 별점·순위·점수·"추천"·"AI" 표현을 사용하지 않는다(F5 원칙, 이 화면에도 동일 적용).
- 순수 로직(`lib/stores/distance.ts`)은 유닛테스트한다. 브라우저 전용 UI(카드 확장, 지도 SDK 로딩·렌더링, 위치 권한 요청)는 이 프로젝트의 기존 컨벤션대로 자동화 테스트 없이 `npm run dev`로 수동 검증한다.
- `lib/stores/types.ts`의 기존 `StoreWithBadge` 타입(`{ name, roadAddress, jibunAddress, lat, lng, firstPrizeAutoCount: number | null }`)을 그대로 재사용한다 — 새 타입을 만들지 않는다.

---

## Task 1: 거리/정렬/지역 순수 함수

**Files:**
- Create: `lib/stores/distance.ts`
- Test: `lib/stores/distance.test.ts`

**Interfaces:**
- Consumes: `StoreWithBadge` from `lib/stores/types.ts`(F4 데이터 파이프라인에서 이미 존재).
- Produces: `haversineDistanceKm(a, b): number`, `extractRegion(roadAddress): string`, `extractRegions(stores): string[]`, `sortStoresByCount(stores): StoreWithBadge[]`, `sortStoresByDistance(stores, origin): StoreWithBadge[]`, `filterStoresByRegion(stores, region): StoreWithBadge[]` — Task 3이 사용.

- [ ] **Step 1: 실패하는 테스트 작성**

```ts
// lib/stores/distance.test.ts
import { describe, it, expect } from 'vitest';
import {
  haversineDistanceKm,
  extractRegion,
  extractRegions,
  sortStoresByCount,
  sortStoresByDistance,
  filterStoresByRegion,
} from './distance';
import type { StoreWithBadge } from './types';

describe('haversineDistanceKm', () => {
  it('returns 0 for the same point', () => {
    expect(haversineDistanceKm({ lat: 37.5665, lng: 126.978 }, { lat: 37.5665, lng: 126.978 })).toBe(0);
  });

  it('computes ~111.2km for 1 degree of longitude at the equator', () => {
    const d = haversineDistanceKm({ lat: 0, lng: 0 }, { lat: 0, lng: 1 });
    expect(d).toBeGreaterThan(110);
    expect(d).toBeLessThan(112);
  });

  it('is symmetric', () => {
    const a = { lat: 37.5, lng: 127.0 };
    const b = { lat: 35.2, lng: 129.1 };
    expect(haversineDistanceKm(a, b)).toBeCloseTo(haversineDistanceKm(b, a), 10);
  });
});

describe('extractRegion', () => {
  it('extracts the leading whitespace-separated token', () => {
    expect(extractRegion('서울특별시 강남구 테헤란로 1')).toBe('서울특별시');
  });

  it('returns an empty string for an empty address', () => {
    expect(extractRegion('')).toBe('');
  });
});

describe('extractRegions', () => {
  it('returns unique sorted regions from a store list', () => {
    const stores = [
      { roadAddress: '서울특별시 강남구 1' },
      { roadAddress: '부산광역시 해운대구 1' },
      { roadAddress: '서울특별시 서초구 1' },
    ] as StoreWithBadge[];
    expect(extractRegions(stores)).toEqual(['부산광역시', '서울특별시']);
  });
});

function store(overrides: Partial<StoreWithBadge> = {}): StoreWithBadge {
  return {
    name: '테스트복권방',
    roadAddress: '서울특별시 강남구 1',
    jibunAddress: '서울특별시 강남구 1-1',
    lat: 37.5,
    lng: 127.0,
    firstPrizeAutoCount: null,
    ...overrides,
  };
}

describe('sortStoresByCount', () => {
  it('sorts descending by firstPrizeAutoCount, treating null as 0', () => {
    const stores = [
      store({ name: 'A', firstPrizeAutoCount: 1 }),
      store({ name: 'B', firstPrizeAutoCount: null }),
      store({ name: 'C', firstPrizeAutoCount: 5 }),
    ];
    expect(sortStoresByCount(stores).map((s) => s.name)).toEqual(['C', 'A', 'B']);
  });

  it('does not mutate the input array', () => {
    const stores = [store({ name: 'A', firstPrizeAutoCount: 1 }), store({ name: 'B', firstPrizeAutoCount: 5 })];
    const original = [...stores];
    sortStoresByCount(stores);
    expect(stores).toEqual(original);
  });
});

describe('sortStoresByDistance', () => {
  it('sorts ascending by distance from the origin', () => {
    const near = store({ name: 'near', lat: 37.5, lng: 127.0 });
    const far = store({ name: 'far', lat: 35.2, lng: 129.1 });
    const result = sortStoresByDistance([far, near], { lat: 37.5, lng: 127.0 });
    expect(result.map((s) => s.name)).toEqual(['near', 'far']);
  });
});

describe('filterStoresByRegion', () => {
  it('keeps only stores whose leading address token matches the region', () => {
    const stores = [
      store({ name: 'A', roadAddress: '서울특별시 강남구 1' }),
      store({ name: 'B', roadAddress: '부산광역시 해운대구 1' }),
    ];
    expect(filterStoresByRegion(stores, '서울특별시').map((s) => s.name)).toEqual(['A']);
  });
});
```

- [ ] **Step 2: 테스트 실행해서 실패 확인**

Run: `npm run test -- lib/stores/distance.test.ts`
Expected: FAIL (`distance.ts` 모듈이 없음)

- [ ] **Step 3: 구현 작성**

```ts
// lib/stores/distance.ts
import type { StoreWithBadge } from './types';

const EARTH_RADIUS_KM = 6371;

function toRadians(deg: number): number {
  return (deg * Math.PI) / 180;
}

export function haversineDistanceKm(
  a: { lat: number; lng: number },
  b: { lat: number; lng: number },
): number {
  const dLat = toRadians(b.lat - a.lat);
  const dLng = toRadians(b.lng - a.lng);
  const lat1 = toRadians(a.lat);
  const lat2 = toRadians(b.lat);
  const h =
    Math.sin(dLat / 2) ** 2 + Math.cos(lat1) * Math.cos(lat2) * Math.sin(dLng / 2) ** 2;
  return EARTH_RADIUS_KM * 2 * Math.asin(Math.sqrt(h));
}

export function extractRegion(roadAddress: string): string {
  return roadAddress.trim().split(/\s+/)[0] ?? '';
}

export function extractRegions(stores: Pick<StoreWithBadge, 'roadAddress'>[]): string[] {
  const set = new Set(stores.map((s) => extractRegion(s.roadAddress)).filter(Boolean));
  return Array.from(set).sort();
}

export function sortStoresByCount(stores: StoreWithBadge[]): StoreWithBadge[] {
  return [...stores].sort((a, b) => (b.firstPrizeAutoCount ?? 0) - (a.firstPrizeAutoCount ?? 0));
}

export function sortStoresByDistance(
  stores: StoreWithBadge[],
  origin: { lat: number; lng: number },
): StoreWithBadge[] {
  return [...stores].sort(
    (a, b) => haversineDistanceKm(origin, a) - haversineDistanceKm(origin, b),
  );
}

export function filterStoresByRegion(stores: StoreWithBadge[], region: string): StoreWithBadge[] {
  return stores.filter((s) => extractRegion(s.roadAddress) === region);
}
```

- [ ] **Step 4: 테스트 실행해서 통과 확인**

Run: `npm run test -- lib/stores/distance.test.ts`
Expected: PASS (10 tests)

- [ ] **Step 5: 커밋**

```bash
git add lib/stores/distance.ts lib/stores/distance.test.ts
git commit -m "feat: add distance/sort/region utilities for store finder"
```

## Task 2: 판매점 카드 컴포넌트

**Files:**
- Create: `components/lotto/StoreCard.tsx`

**Interfaces:**
- Consumes: `StoreWithBadge` from `lib/stores/types.ts`; `haversineDistanceKm` from `lib/stores/distance.ts`(Task 1).
- Produces: `StoreCard({ store, userPosition }): JSX.Element` — Task 3(`StoreListView`)이 사용.

- [ ] **Step 1: 컴포넌트 작성**

```tsx
// components/lotto/StoreCard.tsx
'use client';

import { useState } from 'react';
import type { StoreWithBadge } from '@/lib/stores/types';
import { haversineDistanceKm } from '@/lib/stores/distance';

export function StoreCard({
  store,
  userPosition,
}: {
  store: StoreWithBadge;
  userPosition: { lat: number; lng: number } | null;
}) {
  const [expanded, setExpanded] = useState(false);

  const distanceLabel = userPosition
    ? `${haversineDistanceKm(userPosition, store).toFixed(1)}km`
    : null;

  const directionsUrl = `https://map.kakao.com/link/to/${encodeURIComponent(store.name)},${store.lat},${store.lng}`;

  return (
    <div className="rounded-xl bg-white shadow-sm p-4">
      <button
        type="button"
        onClick={() => setExpanded((v) => !v)}
        className="w-full flex items-center justify-between gap-3 text-left"
      >
        <div className="min-w-0">
          <p className="font-semibold truncate">{store.name}</p>
          <p className="text-xs text-gray-400 truncate">{distanceLabel ?? store.roadAddress}</p>
        </div>
        {store.firstPrizeAutoCount !== null && (
          <span className="text-xs px-2 py-1 rounded-full bg-amber-100 text-amber-700 shrink-0">
            1등 {store.firstPrizeAutoCount}회 배출
          </span>
        )}
      </button>

      {expanded && (
        <div className="mt-3 pt-3 border-t space-y-2 text-sm text-gray-600">
          <p>{store.roadAddress}</p>
          {store.firstPrizeAutoCount !== null && (
            <p className="text-xs text-gray-400">
              자동선택 당첨 기준입니다. 특정 판매점 이용이 당첨 확률에 영향을 주지 않습니다.
            </p>
          )}
          <a
            href={directionsUrl}
            target="_blank"
            rel="noopener noreferrer"
            className="inline-block text-sm px-3 py-1 rounded-lg bg-gray-100 hover:bg-gray-200"
          >
            길찾기
          </a>
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 2: 타입체크 확인**

Run: `npx tsc --noEmit`
Expected: 에러 없음.

- [ ] **Step 3: 커밋**

```bash
git add components/lotto/StoreCard.tsx
git commit -m "feat: add StoreCard component with inline detail expansion"
```

## Task 3: 판매점 페이지 — 리스트 뷰 + 위치/정렬/지역 필터

**Files:**
- Create: `app/stores/page.tsx`
- Create: `components/lotto/StoreListView.tsx`
- Create: `components/lotto/StoreMapView.tsx` (이 태스크에서는 플레이스홀더만 — Task 4가 실제 구현으로 교체)

**Interfaces:**
- Consumes: `StoreWithBadge` from `lib/stores/types.ts`; `extractRegions`, `filterStoresByRegion`, `sortStoresByCount`, `sortStoresByDistance` from `lib/stores/distance.ts`(Task 1); `StoreCard` from `components/lotto/StoreCard.tsx`(Task 2).
- Produces: `StoreListView({ stores, userPosition }): JSX.Element` — 이 태스크 자체가 사용. `StoreMapView({ stores, userPosition }): JSX.Element` 플레이스홀더 — Task 4가 실제 구현으로 교체(props 시그니처는 이 태스크에서 확정하고 Task 4는 그대로 유지).

- [ ] **Step 1: StoreListView 작성**

```tsx
// components/lotto/StoreListView.tsx
import { StoreCard } from './StoreCard';
import type { StoreWithBadge } from '@/lib/stores/types';

export function StoreListView({
  stores,
  userPosition,
}: {
  stores: StoreWithBadge[];
  userPosition: { lat: number; lng: number } | null;
}) {
  if (stores.length === 0) {
    return <p className="text-sm text-gray-500 text-center py-8">해당 지역에 표시할 판매점이 없습니다.</p>;
  }

  return (
    <div className="space-y-2">
      {stores.map((store) => (
        <StoreCard key={`${store.name}-${store.roadAddress}`} store={store} userPosition={userPosition} />
      ))}
    </div>
  );
}
```

- [ ] **Step 2: StoreMapView 플레이스홀더 작성**

```tsx
// components/lotto/StoreMapView.tsx
import type { StoreWithBadge } from '@/lib/stores/types';

export function StoreMapView({
  stores,
  userPosition,
}: {
  stores: StoreWithBadge[];
  userPosition: { lat: number; lng: number } | null;
}) {
  void stores;
  void userPosition;
  return (
    <div className="w-full h-[60vh] rounded-xl bg-white shadow-sm flex items-center justify-center text-sm text-gray-400">
      지도 뷰 준비 중입니다.
    </div>
  );
}
```

- [ ] **Step 3: 판매점 페이지 작성**

```tsx
// app/stores/page.tsx
'use client';

import { useMemo, useState } from 'react';
import storesData from '@/data/lotto-stores.json';
import type { StoreWithBadge } from '@/lib/stores/types';
import {
  extractRegions,
  filterStoresByRegion,
  sortStoresByCount,
  sortStoresByDistance,
} from '@/lib/stores/distance';
import { StoreListView } from '@/components/lotto/StoreListView';
import { StoreMapView } from '@/components/lotto/StoreMapView';

const stores = storesData as StoreWithBadge[];

type LocationStatus = 'idle' | 'granted' | 'denied' | 'unsupported';

export default function StoresPage() {
  const [view, setView] = useState<'list' | 'map'>('list');
  const [locationStatus, setLocationStatus] = useState<LocationStatus>('idle');
  const [userPosition, setUserPosition] = useState<{ lat: number; lng: number } | null>(null);
  const [sortBy, setSortBy] = useState<'distance' | 'count'>('count');
  const [regionFilter, setRegionFilter] = useState<string | null>(null);

  const regions = useMemo(() => extractRegions(stores), []);

  function requestLocation() {
    if (!('geolocation' in navigator)) {
      setLocationStatus('unsupported');
      return;
    }
    navigator.geolocation.getCurrentPosition(
      (pos) => {
        setUserPosition({ lat: pos.coords.latitude, lng: pos.coords.longitude });
        setLocationStatus('granted');
        setSortBy('distance');
      },
      () => {
        setLocationStatus('denied');
      },
    );
  }

  const visibleStores = useMemo(() => {
    let result = stores;
    if (locationStatus !== 'granted' && regionFilter) {
      result = filterStoresByRegion(result, regionFilter);
    }
    if (sortBy === 'distance' && userPosition) {
      return sortStoresByDistance(result, userPosition);
    }
    return sortStoresByCount(result);
  }, [locationStatus, regionFilter, sortBy, userPosition]);

  return (
    <main className="min-h-screen bg-gray-100">
      <div className="max-w-2xl mx-auto p-4 sm:p-6 space-y-4">
        <h1 className="text-2xl font-bold">판매점 찾기</h1>

        <p className="text-xs text-gray-500">
          판매점 목록은 2025-06-07 기준이며, 폐업·신규 매장이 반영되지 않았을 수 있습니다. 1등 배출 이력은
          2025년 6월 기준입니다.
        </p>

        <div className="flex gap-2">
          <button
            type="button"
            onClick={() => setView('list')}
            className={`flex-1 py-2 rounded-lg text-sm transition-shadow ${view === 'list' ? 'bg-black text-white shadow-md' : 'bg-white shadow-sm hover:shadow'}`}
          >
            리스트 뷰
          </button>
          <button
            type="button"
            onClick={() => setView('map')}
            className={`flex-1 py-2 rounded-lg text-sm transition-shadow ${view === 'map' ? 'bg-black text-white shadow-md' : 'bg-white shadow-sm hover:shadow'}`}
          >
            지도 뷰
          </button>
        </div>

        {locationStatus === 'granted' ? (
          <div className="flex gap-2">
            <button
              type="button"
              onClick={() => setSortBy('distance')}
              className={`flex-1 py-2 rounded-lg text-sm ${sortBy === 'distance' ? 'bg-black text-white' : 'bg-white shadow-sm'}`}
            >
              거리순
            </button>
            <button
              type="button"
              onClick={() => setSortBy('count')}
              className={`flex-1 py-2 rounded-lg text-sm ${sortBy === 'count' ? 'bg-black text-white' : 'bg-white shadow-sm'}`}
            >
              배출횟수순
            </button>
          </div>
        ) : (
          <button
            type="button"
            onClick={requestLocation}
            className="w-full py-2 rounded-lg text-sm bg-white shadow-sm hover:shadow"
          >
            내 위치로 정렬
          </button>
        )}

        {(locationStatus === 'denied' || locationStatus === 'unsupported') && (
          <select
            value={regionFilter ?? ''}
            onChange={(e) => setRegionFilter(e.target.value || null)}
            className="w-full bg-white rounded-lg px-3 py-2 text-sm shadow-inner"
          >
            <option value="">전체 지역</option>
            {regions.map((r) => (
              <option key={r} value={r}>
                {r}
              </option>
            ))}
          </select>
        )}

        {view === 'list' ? (
          <StoreListView stores={visibleStores} userPosition={userPosition} />
        ) : (
          <StoreMapView stores={visibleStores} userPosition={userPosition} />
        )}
      </div>
    </main>
  );
}
```

- [ ] **Step 4: 개발 서버로 수동 검증**

Run: `npm run dev`, 브라우저에서 `http://localhost:3000/stores` 접속.

Expected:
- 리스트 뷰가 기본으로 보이고, 배출횟수순(1등 배출 많은 순)으로 정렬되어 있다.
- 상단에 데이터 기준일 문구가 정확히 표시된다.
- 판매점 카드 클릭 시 그 자리에서 확장되어 주소·(배지 있으면) 안내 문구·길찾기 링크가 나온다. 길찾기 링크 클릭 시 새 탭으로 카카오맵이 열린다.
- "내 위치로 정렬" 클릭 → 브라우저 위치 권한 팝업이 뜬다.
  - **허용** 시: 거리순으로 재정렬되고, 버튼이 `[거리순 | 배출횟수순]` 토글로 바뀐다. 각 카드에 거리(km)가 주소 대신 표시된다.
  - **거부** 시: 지역 드롭다운이 나타난다. 드롭다운을 바꾸면 해당 지역 판매점만 보인다(현재 데이터가 서울 위주라 "서울특별시" 정도만 보일 수 있음 — 정상).
- "지도 뷰" 탭 클릭 시 "지도 뷰 준비 중입니다." 플레이스홀더가 보인다(Task 4에서 실제 지도로 교체 예정).

브라우저 개발자도구에서 위치 권한 상태를 초기화(사이트 설정에서 위치 권한 리셋)하면 허용/거부 두 경로를 모두 테스트할 수 있다.

- [ ] **Step 5: 커밋**

```bash
git add app/stores/page.tsx components/lotto/StoreListView.tsx components/lotto/StoreMapView.tsx
git commit -m "feat: add store finder page with list view, location, and sorting"
```

## Task 4: 지도 뷰 (Kakao Maps SDK + 클러스터링)

**Files:**
- Modify: `components/lotto/StoreMapView.tsx` (Task 3의 플레이스홀더를 실제 구현으로 교체)
- Modify: `.env` (플레이스홀더 키 추가)

**Interfaces:**
- Consumes: `StoreWithBadge` from `lib/stores/types.ts`. Props 시그니처는 Task 3에서 이미 확정됨(`{ stores, userPosition }`) — 변경하지 않는다.

**사용자 작업 (이 태스크 시작 전 필요):**
- Kakao Developers 앱의 **JavaScript 키**(REST 키와 다름, 이미 발급·도메인 등록 완료로 확인됨)를 준비해둔다.

- [ ] **Step 1: `.env`에 플레이스홀더 추가**

`.env` 파일에 아래 줄을 추가한다(값은 비워두고, 사용자가 직접 채운다). `NEXT_PUBLIC_` 접두사가 필요하다 — 브라우저에서 직접 쓰는 값이라 Next.js가 클라이언트 번들에 포함시켜야 하기 때문이다:

```
NEXT_PUBLIC_KAKAO_JS_KEY=
```

- [ ] **Step 2: StoreMapView 실제 구현으로 교체**

```tsx
// components/lotto/StoreMapView.tsx
'use client';

import { useEffect, useRef } from 'react';
import type { StoreWithBadge } from '@/lib/stores/types';

interface KakaoLatLng {}
interface KakaoMap {}
interface KakaoMarker {}
interface KakaoInfoWindow {
  open: (map: KakaoMap, marker: KakaoMarker) => void;
}
interface KakaoMarkerClusterer {
  addMarkers: (markers: KakaoMarker[]) => void;
}
interface KakaoMapsNamespace {
  LatLng: new (lat: number, lng: number) => KakaoLatLng;
  Map: new (container: HTMLElement, options: { center: KakaoLatLng; level: number }) => KakaoMap;
  Marker: new (options: { position: KakaoLatLng }) => KakaoMarker;
  InfoWindow: new (options: { content: string }) => KakaoInfoWindow;
  MarkerClusterer: new (options: {
    map: KakaoMap;
    averageCenter: boolean;
    minLevel: number;
  }) => KakaoMarkerClusterer;
  event: { addListener: (target: KakaoMarker, type: string, handler: () => void) => void };
  load: (callback: () => void) => void;
}

declare global {
  interface Window {
    kakao?: { maps: KakaoMapsNamespace };
  }
}

const KAKAO_SDK_ID = 'kakao-maps-sdk';

function loadKakaoMapsSdk(appKey: string): Promise<KakaoMapsNamespace> {
  return new Promise((resolve, reject) => {
    if (window.kakao?.maps) {
      resolve(window.kakao.maps);
      return;
    }
    const existing = document.getElementById(KAKAO_SDK_ID);
    if (existing) {
      existing.addEventListener('load', () => window.kakao!.maps.load(() => resolve(window.kakao!.maps)));
      return;
    }
    const script = document.createElement('script');
    script.id = KAKAO_SDK_ID;
    script.src = `https://dapi.kakao.com/v2/maps/sdk.js?appkey=${appKey}&libraries=clusterer&autoload=false`;
    script.onload = () => window.kakao!.maps.load(() => resolve(window.kakao!.maps));
    script.onerror = () => reject(new Error('Kakao Maps SDK 로드 실패'));
    document.head.appendChild(script);
  });
}

export function StoreMapView({
  stores,
  userPosition,
}: {
  stores: StoreWithBadge[];
  userPosition: { lat: number; lng: number } | null;
}) {
  const containerRef = useRef<HTMLDivElement>(null);
  const appKey = process.env.NEXT_PUBLIC_KAKAO_JS_KEY;

  useEffect(() => {
    if (!appKey || !containerRef.current) return;

    let cancelled = false;

    loadKakaoMapsSdk(appKey).then((kakaoMaps) => {
      if (cancelled || !containerRef.current) return;

      const center = userPosition
        ? new kakaoMaps.LatLng(userPosition.lat, userPosition.lng)
        : new kakaoMaps.LatLng(37.5665, 126.978);

      const map = new kakaoMaps.Map(containerRef.current, { center, level: 7 });

      const markers = stores.map((store) => {
        const marker = new kakaoMaps.Marker({
          position: new kakaoMaps.LatLng(store.lat, store.lng),
        });

        const badgeLine =
          store.firstPrizeAutoCount !== null ? `1등 ${store.firstPrizeAutoCount}회 배출<br/>` : '';
        const directionsUrl = `https://map.kakao.com/link/to/${encodeURIComponent(store.name)},${store.lat},${store.lng}`;
        const infoWindow = new kakaoMaps.InfoWindow({
          content: `<div style="padding:8px;font-size:12px;"><strong>${store.name}</strong><br/>${badgeLine}<a href="${directionsUrl}" target="_blank" rel="noopener noreferrer">길찾기</a></div>`,
        });

        kakaoMaps.event.addListener(marker, 'click', () => {
          infoWindow.open(map, marker);
        });

        return marker;
      });

      const clusterer = new kakaoMaps.MarkerClusterer({ map, averageCenter: true, minLevel: 6 });
      clusterer.addMarkers(markers);
    });

    return () => {
      cancelled = true;
    };
  }, [stores, userPosition, appKey]);

  if (!appKey) {
    return (
      <div className="w-full h-[60vh] rounded-xl bg-white shadow-sm flex items-center justify-center text-sm text-gray-400 text-center px-4">
        지도를 표시하려면 NEXT_PUBLIC_KAKAO_JS_KEY 환경변수가 필요합니다.
      </div>
    );
  }

  return <div ref={containerRef} className="w-full h-[60vh] rounded-xl overflow-hidden shadow-sm" />;
}
```

- [ ] **Step 3: 타입체크 확인**

Run: `npx tsc --noEmit`
Expected: 에러 없음.

- [ ] **Step 4: 개발 서버로 수동 검증**

`.env`의 `NEXT_PUBLIC_KAKAO_JS_KEY`에 실제 키를 채운 뒤:

Run: `npm run dev`, `http://localhost:3000/stores` 접속 후 "지도 뷰" 탭 클릭.

Expected:
- 지도가 로드되고, 판매점 위치에 마커가 표시된다.
- 마커가 가까이 모여 있으면 클러스터(숫자가 적힌 원)로 묶여 표시된다(레벨을 줌인하면 개별 마커로 풀림).
- 마커 클릭 시 InfoWindow가 열리고 상호명·(있으면) 배지·길찾기 링크가 보인다. 링크 클릭 시 새 탭으로 카카오맵이 열린다.
- "내 위치로 정렬"을 허용한 상태에서 지도 뷰로 전환하면 지도 중심이 사용자 위치 근처로 잡힌다.

- [ ] **Step 5: 커밋**

```bash
git add components/lotto/StoreMapView.tsx
git commit -m "feat: implement Kakao Maps view with marker clustering"
```

## Task 5: 홈 화면 진입 카드 + 전체 검증

**Files:**
- Modify: `app/page.tsx`

**Interfaces:**
- Consumes: 없음(단순 링크 추가).

- [ ] **Step 1: 홈 화면에 판매점 찾기 진입 카드 추가**

`app/page.tsx`에서 "역대 최다 출현 TOP 6" 카드와 `<Disclaimer />` 사이에 아래 블록을 추가한다(정확한 삽입 위치는 파일을 열어 두 요소 사이를 확인한다):

```tsx
      <Link
        href="/stores"
        className="block text-center bg-black text-white rounded-lg py-3 font-semibold"
      >
        판매점 찾기
      </Link>
```

기존에 이미 `import Link from 'next/link';`가 있으므로 추가 import는 필요 없다.

- [ ] **Step 2: 개발 서버로 수동 검증**

Run: `npm run dev`, `http://localhost:3000/` 접속.

Expected: "판매점 찾기" 버튼이 보이고, 클릭 시 `/stores` 페이지로 이동한다.

- [ ] **Step 3: 커밋**

```bash
git add app/page.tsx
git commit -m "feat: add store finder entry link to home page"
```

## Task 6: 전체 검증

- [ ] **Step 1: 전체 테스트 스위트 실행 (회귀 확인)**

Run: `npm run test`
Expected: 기존 테스트 전부 PASS + 이번에 추가한 `distance.test.ts`(10개) = 이전 57개 + 10개 = 67개 PASS.

- [ ] **Step 2: 린트/빌드 확인**

Run: `npm run lint && npm run build`
Expected: 에러 없이 완료.

- [ ] **Step 3: 최종 커밋**

```bash
git add -A
git commit -m "chore: verify F4 store UI tests, lint, and build pass" --allow-empty
```

---

## 이 계획 밖의 후속 작업 (사용자가 직접 진행)

1. **Vercel production 환경변수 등록** — `NEXT_PUBLIC_KAKAO_JS_KEY`를 `vercel env add`로 production에 등록한다(로컬 `.env`와 별개로 배포 환경에도 필요).
2. **전체 8,802건 지오코딩 완료** — F4 데이터 파이프라인 계획의 후속 작업으로, 이 UI 계획과 독립적으로 아무 때나 진행 가능하다. 데이터가 늘어나면 이 UI는 코드 변경 없이 더 많은 마커/카드를 그대로 보여준다.
